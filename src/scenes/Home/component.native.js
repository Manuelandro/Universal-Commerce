import React, { PropTypes } from 'react'
import { Spinner, ErrorMsg, Product, Card, Text } from '../../components/native/'

const Home = (props) =>
    <Card>
        <Text>Home</Text>
    </Card>

export default Home